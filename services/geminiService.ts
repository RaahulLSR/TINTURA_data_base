// AI Features have been removed from this application.
// This file is kept as a placeholder to prevent import errors in legacy code,
// though no active code uses it anymore.

export const generateReportSummary = async (dataContext: string): Promise<string> => {
  return "AI features are disabled.";
};

export const predictDelays = async (orders: any[]): Promise<string> => {
  return "AI features are disabled.";
};